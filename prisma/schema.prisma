// Schema de base de datos — a definir una vez completado el análisis del sistema actual

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
